# include "IMAGE.h"
# include <iostream>
# include <cstdlib>
# include <cstring>
# include <string>
# include <fstream>

//Incomplete project

using namespace std;

int main(void)
{
	/*char *imageName = new char[10];
	strcpy(imageName, "blend.bmp");

	IMAGE *image = new IMAGE(imageName);*/

	/*
	strcpy(imageName, "blended.bmp");
	IMAGE *blended = new IMAGE(imageName);

	IMAGE *blend = new IMAGE(imageName);
	
		*blend = IMAGE_BLENDING( &(*image), &(*blend), 0.8, imageName);

	strcpy(imageName, "blended.ppm");
	blended->EXPORT(imageName);
*/
	/*cout << " IMAGE() SUCCESS";
	
	strcpy(imageName, "galaxy.ppm");
	image->EXPORT(imageName);

	image->BLACK_WHITE();
	strcpy(imageName,"galaxy_gray_log.pgm");
	image->GRAYSCALE_LOGARITHM_OPERATOR();
	image->EXPORT_GRAY(imageName);

	/*image->BLUR();
	image->BLUR();
	image->BLUR();
	image->BLUR();
	image->BLUR();
	strcpy(imageName,"im2blur.ppm");*/
	//image->CROP( image->getHeight()/2, image->getWidth()/2, 300, 300);
	/*
	image->BLACK_WHITE();
	strcpy(imageName,"im2gray.pgm");
	image->EXPORT_GRAY(imageName);*/

	

	
	return 0;
}
